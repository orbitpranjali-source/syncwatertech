// PartnersSection was deprecated in favor of dynamic loading directly on ClientsPage.
// Keeping a tiny placeholder to avoid accidental import errors. The real Partners UI
// is implemented in src/pages/ClientsPage.tsx which dynamically loads partner assets.

export default function PartnersSection() {
  return null;
}
